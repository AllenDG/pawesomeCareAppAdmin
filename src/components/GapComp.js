import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function GapComp({ height, width }) {
  return <View style={{ height: height, width: width }}></View>;
}

const styles = StyleSheet.create({});
